import {StyleSheet} from 'react-native';
import React from 'react';
import {ControllTab} from '../components/scanning';

const Processing = () => {
  return <ControllTab />;
};

export default Processing;

const styles = StyleSheet.create({});
